library(dplyr)

permits_demo <- read.csv("permits_demographicdata.csv",
                           header = T,
                           sep = ",")
permits_demo <- permits_demo %>%
  filter()
